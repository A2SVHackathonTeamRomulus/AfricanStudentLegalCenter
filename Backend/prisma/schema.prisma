// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://romulus:123@cluster0.fktecgb.mongodb.net/?retryWrites=true&w=majority"
}
